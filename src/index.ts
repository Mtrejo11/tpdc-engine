/**
 * tpdc-engine v2 — library entry.
 *
 * Public exports for programmatic use. Currently bare while v2 modules
 * are stabilizing. See `~/Documents/Claude/Projects/TPDC/DECISIONS.md`
 * for v2 architecture context.
 */

export const VERSION = "0.2.0-alpha.0" as const;
