/**
 * tpdc-engine v3 — library entry.
 *
 * Public exports for programmatic use. Currently bare while the v0.3 MCP
 * pivot stabilizes. See `~/Documents/Claude/Projects/TPDC/VISION.md` for
 * the source-of-truth architecture.
 */

export const VERSION = "0.3.0-alpha.4" as const;
