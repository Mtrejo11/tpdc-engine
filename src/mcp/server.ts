#!/usr/bin/env node
/**
 * tpdc MCP server — v2 stub.
 *
 * Will expose fine-grained tools per DECISIONS.md "Decisiones derivadas":
 *   tpdc_intake, tpdc_plan, tpdc_execute, tpdc_run_tests,
 *   tpdc_open_pr, tpdc_wait_ci, tpdc_show, tpdc_diff, ...
 *
 * Implementation deferred until v2 stages stabilize.
 */

console.error("tpdc-mcp: v2 not implemented yet — see DECISIONS.md");
process.exit(1);
